# Portafolio Jasmin (Next.js)
1. `npm install`
2. `npm run dev` → http://localhost:3000
3. Edita `data/content.js`. Reemplaza `public/foto.svg` y `public/proyecto.svg` por tus imágenes (y actualiza la ruta en `content.js`).
4. Publica gratis con Vercel: sube el repo y conéctalo.
